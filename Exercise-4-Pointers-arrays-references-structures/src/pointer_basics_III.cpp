#include <iostream>

using namespace std;

int main()
{
    // 4.3) Given the string "A string." Print on one line the letter on the
    // index 0, the pointer position and the letter t. undate the pointer to
    // pointer +2. Then, in another line print the pointer and the letters r
    // and g of the string (using the pointer).

    return 0;
}
