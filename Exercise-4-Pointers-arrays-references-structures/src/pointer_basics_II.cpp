#include <iostream>

using namespace std;

int main()
{
    // 4.2)  find the max of an integral data set. The program will ask
    // the user to input the number of data values in the set and each value.
    // The program prints on screen a pointer that points to the max value.

    return 0;
}
